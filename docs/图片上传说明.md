# 图片上传说明

[← 返回首页](../README.md)

## 📸 图片CDN加速方案

本项目使用 **jsDelivr CDN** 来加速图片加载，这是一个免费的、基于GitHub的CDN服务，在中国国内访问速度较快。

### 两种CDN方案

本项目支持两种CDN方案，**两种方案都可以正常工作**：

#### 方案一：使用专门的CDN仓库（推荐）⭐

- **CDN仓库**：`git@github.com:dadongshangu/CDN.git`
- **优点**：
  - 图片资源集中管理
  - 不影响主仓库大小
  - 更专业的资源管理方式
  - 便于复用图片资源
- **CDN链接格式**：
  ```
  https://cdn.jsdelivr.net/gh/dadongshangu/CDN@master/qiuxuan/图片文件名.png
  ```

#### 方案二：使用当前仓库

- **当前仓库**：`git@github.com:dadongshangu/qiuxuan.git`
- **优点**：
  - 图片和文档在同一仓库，便于管理
  - 不需要额外的仓库
- **CDN链接格式**：
  ```
  https://cdn.jsdelivr.net/gh/dadongshangu/qiuxuan@master/assets/images/图片文件名.png
  ```

**注意**：两种方案可以同时使用，互不影响。**当前项目已切换到方案一（专门的CDN仓库）**，所有图片链接已更新为CDN仓库格式。

## 🚀 如何上传图片

### 方案一：上传到专门的CDN仓库（推荐）⭐

#### 步骤1：配置CDN仓库（使用稀疏检出，推荐）⭐

**为什么使用稀疏检出？**
- CDN仓库可能包含大量文件，完整克隆时网络不稳定
- 稀疏检出只下载 `qiuxuan/` 目录，大幅减少下载量
- 避免网络超时问题

**自动配置（最简单）**：

```powershell
# 在qiuxuan项目目录中运行
cd E:\3.github\repositories\qiuxuan
.\scripts\setup_cdn_sparse_checkout.ps1
```

脚本会自动完成所有配置。

**手动配置**：

```powershell
# 1. 进入CDN仓库目录
cd E:\3.github\repositories\CDN

# 2. 初始化Git仓库（如果还没有）
git init
git remote add origin git@github.com:dadongshangu/CDN.git

# 3. 启用稀疏检出
git config core.sparseCheckout true

# 4. 配置只检出qiuxuan目录
echo "qiuxuan/*" > .git/info/sparse-checkout

# 5. 拉取（只下载qiuxuan目录）
git pull origin master --allow-unrelated-histories
```

**注意**：如果远程仓库中还没有 `qiuxuan` 目录，拉取可能会失败，但这是正常的。你可以直接创建目录并上传图片。

#### 步骤1（备选）：完整克隆CDN仓库（不推荐）

如果CDN仓库文件不多，也可以完整克隆：

```bash
# 如果还没有克隆CDN仓库
git clone git@github.com:dadongshangu/CDN.git
cd CDN

# 或者如果已经克隆，直接进入目录
cd CDN
```

**注意**：如果遇到网络问题（如 `fetch-pack: unexpected disconnect`），请使用稀疏检出方法。

#### 步骤2：准备图片文件

1. 在CDN仓库中创建 `qiuxuan/` 目录（如果不存在）
2. 将图片文件保存到 `qiuxuan/` 目录
3. 建议使用 `.png` 或 `.jpg` 格式
4. 文件名使用英文和下划线，例如：`physics_question_13.png`

#### 步骤3：提交图片到CDN仓库

```bash
# 在CDN仓库目录中
git add qiuxuan/你的图片文件名.png

# 提交更改
git commit -m "Add qiuxuan image: 你的图片文件名"

# 推送到GitHub
git push origin master
```

#### 步骤4：在文档中引用图片

使用以下格式的CDN链接：

```markdown
![图片描述](https://cdn.jsdelivr.net/gh/dadongshangu/CDN@master/qiuxuan/你的图片文件名.png)
```

**格式说明**：
- `dadongshangu` - GitHub用户名
- `CDN` - CDN仓库名
- `master` - 分支名（如果使用main分支，请改为main）
- `qiuxuan/你的图片文件名.png` - 图片在CDN仓库中的路径

---

### 方案二：上传到当前仓库

#### 步骤1：准备图片文件

1. 将图片文件保存到 `assets/images/` 目录
2. 建议使用 `.png` 或 `.jpg` 格式
3. 文件名使用英文和下划线，例如：`physics_question_13.png`

#### 步骤2：提交图片到GitHub

```bash
# 在qiuxuan仓库目录中
git add assets/images/你的图片文件名.png

# 提交更改
git commit -m "Add image: 你的图片文件名"

# 推送到GitHub
git push origin master
```

#### 步骤3：在文档中引用图片

使用以下格式的CDN链接：

```markdown
![图片描述](https://cdn.jsdelivr.net/gh/dadongshangu/qiuxuan@master/assets/images/你的图片文件名.png)
```

**格式说明**：
- `dadongshangu` - GitHub用户名
- `qiuxuan` - 仓库名
- `master` - 分支名（如果使用main分支，请改为main）
- `assets/images/你的图片文件名.png` - 图片在仓库中的路径

## 📝 示例

### 方案一示例（使用CDN仓库）

假设你上传了一张名为 `math_problem_1.png` 的图片到CDN仓库：

1. **文件位置**：在CDN仓库的 `qiuxuan/math_problem_1.png`
2. **CDN链接**：`https://cdn.jsdelivr.net/gh/dadongshangu/CDN@master/qiuxuan/math_problem_1.png`
3. **Markdown引用**：
   ```markdown
   ![数学题1](https://cdn.jsdelivr.net/gh/dadongshangu/CDN@master/qiuxuan/math_problem_1.png)
   ```

### 方案二示例（使用当前仓库）

假设你上传了一张名为 `math_problem_1.png` 的图片到当前仓库：

1. **文件位置**：`assets/images/math_problem_1.png`
2. **CDN链接**：`https://cdn.jsdelivr.net/gh/dadongshangu/qiuxuan@master/assets/images/math_problem_1.png`
3. **Markdown引用**：
   ```markdown
   ![数学题1](https://cdn.jsdelivr.net/gh/dadongshangu/qiuxuan@master/assets/images/math_problem_1.png)
   ```

## ⚡ CDN优势

- ✅ **免费**：完全免费使用
- ✅ **快速**：国内访问速度较快
- ✅ **稳定**：基于GitHub，稳定可靠
- ✅ **自动更新**：GitHub推送后自动更新
- ✅ **无需注册**：不需要额外注册账号

## 🔍 验证图片是否可用

上传图片后，可以在浏览器中直接访问CDN链接来验证：

**方案一（CDN仓库）**：
```
https://cdn.jsdelivr.net/gh/dadongshangu/CDN@master/qiuxuan/你的图片文件名.png
```

**方案二（当前仓库）**：
```
https://cdn.jsdelivr.net/gh/dadongshangu/qiuxuan@master/assets/images/你的图片文件名.png
```

如果能看到图片，说明上传成功！

## 📌 注意事项

1. **文件大小**：建议单张图片不超过5MB，以保证加载速度
2. **文件格式**：推荐使用 `.png`（适合图表）或 `.jpg`（适合照片）
3. **命名规范**：使用英文和下划线，避免中文和特殊字符
4. **提交时机**：图片文件需要先提交到GitHub，CDN才能访问

## 🛠️ 故障排查

### 图片无法显示

1. **检查文件是否已提交**：
   ```bash
   # 方案一（CDN仓库）
   cd E:\3.github\repositories\CDN
   git ls-files qiuxuan/
   
   # 方案二（当前仓库）
   git ls-files assets/images/
   ```

2. **检查GitHub仓库**：
   - **方案一（CDN仓库）**：访问 `https://github.com/dadongshangu/CDN/tree/master/qiuxuan` 查看文件是否存在
   - **方案二（当前仓库）**：访问 `https://github.com/dadongshangu/qiuxuan/tree/master/assets/images` 查看文件是否存在

3. **清除CDN缓存**：jsDelivr会自动更新，但可能需要几分钟时间

4. **检查链接格式**：确保CDN链接中的路径、分支名正确

### Git同步问题

#### 问题1：网络连接失败（fetch-pack: unexpected disconnect）

**症状**：
```
fetch-pack: unexpected disconnect while reading sideband packetts: 0% (1/601)
fatal: early EOF
fatal: fetch-pack: invalid index-pack output
```

**原因**：CDN仓库文件较多，完整克隆时网络不稳定

**解决方案**：使用**稀疏检出**，只下载 `qiuxuan/` 目录

```powershell
# 运行配置脚本
cd E:\3.github\repositories\qiuxuan
.\scripts\setup_cdn_sparse_checkout.ps1
```

#### 问题2：SSH连接失败

**症状**：
```
ssh: Could not resolve hostname github.com: Temporary failure in name resolution
```

**解决方案**：
1. 检查网络连接
2. 检查SSH密钥配置
3. 尝试使用HTTPS方式（需要配置Personal Access Token）

#### 问题3：稀疏检出配置后仍然下载全部文件

**检查配置**：
```powershell
cd E:\3.github\repositories\CDN
# 查看配置
Get-Content .git/info/sparse-checkout

# 应该显示：qiuxuan/*
# 如果不是，重新配置：
echo "qiuxuan/*" > .git/info/sparse-checkout
git read-tree -mu HEAD
```

---

**最后更新**：2026年1月11日
